name: Test
on:
  push:
    branches:
      - '*'
    tags:
      - '*'
  pull_request:
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          make build
      - name: Test
        run: |
          make test
